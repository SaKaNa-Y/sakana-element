{
  "buildCommand": "pnpm -w run build-hooks && pnpm -w run docs:build && cp -r packages/docs/.vitepress/dist ./dist",
  "outputDirectory": "dist",
  "installCommand": "pnpm install",
  "framework": null
}
