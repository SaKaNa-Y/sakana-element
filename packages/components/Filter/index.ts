import { withInstall } from '@sakana-element/utils';
import Filter from './Filter.vue';
import FilterItem from './FilterItem.vue';

export const PxFilter = withInstall(Filter);
export const PxFilterItem = withInstall(FilterItem);

export * from './types';
